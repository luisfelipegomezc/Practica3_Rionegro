<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luisfelipegomezc.practicarionegro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_catedral"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Demografia" />
        <activity android:name=".Turismo" />
        <activity android:name=".Diversion" />
        <activity android:name=".Hospedaje"></activity>
    </application>

</manifest>
